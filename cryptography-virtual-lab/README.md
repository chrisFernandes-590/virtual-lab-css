# Cryptography Virtual Lab

This package contains the RSA experiment module prepared for the assigned group.

The faculty template requires each experiment to be self-contained and submitted through the group's branch/pull request. The RSA module is located at `experiments/RSA/`.

See `experiments/RSA/README.md` for integration and demonstration instructions.
